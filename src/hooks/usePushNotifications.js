/**
 * usePushNotifications — STUBBED for Expo Go
 *
 * expo-notifications remote push was removed from Expo Go in SDK 53.
 * The package throws at module load time in Expo Go, so we skip it entirely.
 *
 * To re-enable: build with `npx expo run:android` / `npx expo run:ios`
 * and restore the full implementation from git history.
 */

export function usePushNotifications() {
  // no-op in Expo Go — will be enabled when building a dev/production build
}
